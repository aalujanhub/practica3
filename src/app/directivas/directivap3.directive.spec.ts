import { Directivap3Directive } from './directivap3.directive';

describe('Directivap3Directive', () => {
  it('should create an instance', () => {
    const directive = new Directivap3Directive();
    expect(directive).toBeTruthy();
  });
});
